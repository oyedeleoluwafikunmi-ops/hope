# Mira Marketplace Demo

A responsive marketplace demo built with HTML, CSS, and vanilla JavaScript.

Open `index.html` in a browser or serve this folder with a local static server. Saved items and cart contents are stored locally in the browser.
